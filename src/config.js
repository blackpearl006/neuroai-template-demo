// Active config. Everything is assembled from the editable files in /content
// (config.yml + *.md + *.csv + citation.bib). See src/lib/content.js.
import config from "./lib/content";

export default config;
