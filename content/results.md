---
eyebrow: "Results"
title: "Results & Discussion"
lede: "The headline numbers, then what they mean. Swap in your own metrics (metrics.csv), table (results-table.csv) and the prose below."
---
### Where the signal lives

The most important regions concentrate in association cortex and key subcortical
structures — consistent across atlases, which suggests the result is not an
artefact of one particular parcellation.

### What it means

A compact convolutional model recovers a robust brain-age signal from raw
structural MRI, and the regions it relies on are anatomically plausible.

> **Discussion.** These numbers are placeholders. Edit `content/results.md`,
> `content/metrics.csv` and `content/results-table.csv` to drop in your real
> findings.
